# Invalid on purpose: an empty driver list. A scenario nothing runs rots silently inside a set that
# keeps reporting green, so the loader refuses it rather than carrying it.
id: invalid-no-driver
title: A scenario no driver is asked to run
drivers: []
steps:
  - type: action
    domain: task
    op: create
    with: { title: SEED }
