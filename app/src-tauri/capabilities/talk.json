{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "talk",
  "description": "what the talk window may do — kept apart from the board's so the theme stays removable (delete the window and its permissions leave with this file)",
  "windows": [
    "talk"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-title"
  ]
}
