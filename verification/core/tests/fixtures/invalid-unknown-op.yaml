# Rejected: `frobnicate` is not a known task action op — the loader fails closed.
id: invalid-unknown-op
title: an op that does not exist
steps:
  - type: action
    domain: task
    op: frobnicate
    with:
      title: T
